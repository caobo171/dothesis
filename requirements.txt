# DoThesis - Python Dependencies
# Minimal dependencies for maximum simplicity
# Version bounds added for security and stability

# === LLM APIs ===
anthropic>=0.20.0,<1.0.0
openai>=1.0.0,<2.0.0
google-genai>=1.0.0,<2.0.0  # Gemini SDK

# === Citation Management ===
pybtex>=0.24.0,<0.25.0      # BibTeX parsing/generation
citeproc-py>=0.6.0,<0.7.0   # Citation formatting (APA, MLA, Chicago, IEEE)
PyYAML>=6.0.0,<7.0.0        # YAML frontmatter parsing

# === Document Export ===
markdown>=3.5.0,<4.0.0      # Markdown parsing
weasyprint>=60.0,<70.0      # PDF generation (no LaTeX needed!)
cairosvg>=2.7.0,<3.0.0      # research-model diagram (SVG→PNG; reuses weasyprint's cairo)
python-docx>=1.0.0,<2.0.0   # Word document generation

# === HTTP Client & Web Scraping ===
requests>=2.31.0,<3.0.0     # For CrossRef API, GPTZero API, web scraping
beautifulsoup4>=4.12.0,<5.0.0  # HTML parsing for title/metadata scraping
lxml>=4.9.0,<6.0.0          # Fast XML/HTML parser for BeautifulSoup

# === Utilities ===
python-dotenv>=1.0.0,<2.0.0 # Load .env files
rich>=13.0.0,<14.0.0        # Pretty terminal output (for scripts)
psutil>=5.9.0,<7.0.0        # System monitoring for health checks
tenacity>=9.1.4,<10.0.0    # Production-grade retry with exponential backoff

# === LangChain 1.x orchestrator (sub-project 1) ===
langchain>=1.3.0,<2
langchain-core>=1.4.0,<2
langgraph>=1.2.0,<2
langgraph-checkpoint>=4.1.0,<5
langgraph-checkpoint-postgres>=3.1.0,<4
langchain-google-genai>=4.1.0,<5

# === Deep agent runtime (v3 architecture — docs/architecture/2026-06-10) ===
# Skills-driven agent replacing graph_v2; pinned minor because the skills/
# backend API surface is still moving upstream.
deepagents>=0.6.8,<0.7
langchain-anthropic>=1.4.0,<2

# === Statistics engine (thesis-stats) ===
# Real PLS-SEM / EFA / mediation / moderation compute for run_stats' new ops,
# shared with fillform. Pure in-process compute (no network) — the run_stats
# sandbox posture is unchanged. Brings pandas/numpy/scipy/statsmodels/
# scikit-learn/factor-analyzer transitively (pinned in the package).
# Vendored as a git submodule at libs/thesis-stats and installed editable, so
# local engine edits apply immediately. `./dev.sh` runs `git submodule update
# --init` for you; a bare checkout needs it once (or clone --recurse-submodules).
-e ./libs/thesis-stats
